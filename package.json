{
  "name": "assassin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Classroom review game — teacher setup and team join pages",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  }
}
